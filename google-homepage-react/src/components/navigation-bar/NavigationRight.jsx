

const links = ['Gmail', 'Images'];

export const NavigationRight = () => {
    return (
        <div className="nav-bar-second-item">
            {links.map((link)=>{   
                return (link) 
            })}
        </div>
        
    )
}
